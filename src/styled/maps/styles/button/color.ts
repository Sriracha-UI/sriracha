import styledMap from "styled-map";
import { theme } from "../../../theme/index";

export default styledMap`
  yellow: ${theme.colors.deepOrange8};
  invert: ${theme.colors.gray8};
  default: ${theme.colors.gray0};
`;
