import styled from "styled-components";
import {
  flexColumnBase,
  cardSizeBase,
  cardStyleBase,
  centerFontBase,
} from "../base";

const Card = styled.div`
  ${flexColumnBase}
  ${cardSizeBase}
  ${cardStyleBase}
  ${centerFontBase}
`;

export default Card;
