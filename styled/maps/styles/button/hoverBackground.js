import styledMap from "styled-map";
import { theme } from "../../../theme";

export default styledMap`
  red: ${theme.colors.red8};
  orange: ${theme.colors.orange8};
  deepOrange: ${theme.colors.deepOrange8};
  brown: ${theme.colors.brown8};
  amber: ${theme.colors.amber8};
  yellow: ${theme.colors.yellow8};
  lime: ${theme.colors.lime8};
  lightGreen: ${theme.colors.lightGreen8};
  green: ${theme.colors.green8};
  teal: ${theme.colors.teal8};
  cyan: ${theme.colors.cyan8};
  lightBlue: ${theme.colors.lightBlue8};
  blue: ${theme.colors.blue8};
  indigo: ${theme.colors.indigo8};
  deepPurple: ${theme.colors.deepPurple8};
  purple: ${theme.colors.purple8};
  pink: ${theme.colors.pink8};
  invert: ${theme.colors.gray4};
  primary: ${theme.palette.primaryDark};
  secondary: ${theme.palette.secondaryDark};
  success: ${theme.palette.successDark};
  warning: ${theme.palette.warningDark};
  info: ${theme.palette.infoDark};
  default: ${theme.colors.gray8};
`;
